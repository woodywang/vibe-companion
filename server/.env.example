# 后端环境变量（复制为 .env.local）

# 数据库 URL：本地文件用 file: 前缀，远程用 libsql:// (Turso)
DATABASE_URL=file:./.data/app.db

# 仅远程 Turso 需要
# DATABASE_AUTH_TOKEN=

# JWT 签名密钥（生产环境务必更换为随机长串）
AUTH_SECRET=dev-secret-change-me-in-production

# 客户端上传 API 的访问基础地址（用于邮件等场景，可选）
NEXT_PUBLIC_API_BASE=http://localhost:3000
